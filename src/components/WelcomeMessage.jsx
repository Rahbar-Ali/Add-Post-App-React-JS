const WelcomeMessage = ({ handleGetPostsClicked }) => {
  return (
    <center>
      <h1 className="welcome-message">There are no posts</h1>
    </center>
  );
};

export default WelcomeMessage;
